<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.5.0/fonts/remixicon.css" rel="stylesheet"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
    <title>Document</title>
</head>
<body class="bg-zinc-800">
    <%- include('./others/nav.ejs') %> 
    <div id="main" class="w-full min-h-screen flex items-start gap-2 justify-center mt-5 py-2 px-10" data-user-id="<%- post.user._id %>">
        <div class="w-[60%] min-h-screen bg-zinc-700 flex flex-col justify-center rounded-lg">
            <div class="bg-zinc-500 cursor-pointer w-full h-14 flex items-center px-6 rounded-lg gap-4">
                <div id="show" class="rounded-full bg-zinc-400 w-12 h-12 overflow-hidden">
                    <img src="/img/uploads/<%- post.user.profileimg %>" alt="">
                </div>
                <h1 id="ushow" class="cursor-pointer font-semibold text-xl">@<%- post.user.username %></h1>
            </div>
            <img class="py-6 px-8  object-cover" src= "/img/uploads/<%= post.img %>" alt="">
        </div>
        <div class="w-[40%] min-h-screen bg-zinc-700 rounded-lg py-14 px-10">
            <h1 class="text-white font-bold text-4xl"><%- post.title %></h1>
            <h1 class="text-gray-400 font-semibold text-2xl mt-3 px-2"><%- post.desc %></h1>
        </div>
    </div>
    <script src="/js/show.js"></script>
    <script src="/js/nav.js"></script>
</body>
</html>